import PlaceholderPage from "@/components/PlaceholderPage";

export default function TransactionLogPage() {
  return (
    <PlaceholderPage
      title="Transaction Log"
      description="Live ledger of all platform transactions, credit transfers, and payout disbursements. This high-integrity log is anchored on-chain for total transparency."
    />
  );
}
